```
如何解决通过 transform 进行 skew 变形 、 rotate 旋转会出现锯齿现象的问题？

通过以下代码设直样式。
-webkit-transform:rotate(-4deg) skew(l0deg) translateZ(0) ;
transform:rotate(-4deg) skew(l0deg) translateZ(0) ;
outline:1px solid rgba(255,255,255,0)
```

