```
使用 window . location.href 有何兼容问题？

IE 或者 Firefox 2.0.x 下 ，可以使用 window.location 或 window.location.href，
但是在 Firefox 1.5.x 下 ， 只能使用 window.location，
所以可以使用 window.location 来代替 window.location.href
```

